on:
  workflow_call:
    inputs:
      image_name:
        type: string
      tag: 
        type: string
   


jobs:
  my_job:
    name: child2 job1
    runs-on: ubuntu-latest
    steps: 
     -  run: echo "hello child2 job1 step"
        name: child2 job step
